# Required for AI answers
GROQ_API_KEY=your_groq_api_key_here

# Optional: change the model used by the assistant
GROQ_MODEL=llama-3.1-8b-instant

# Optional local paths (Render overrides these in render.yaml)
DB_PATH=data/ragora.db
UPLOAD_DIR=data/documents
